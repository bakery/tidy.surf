{
  "name": "tides",
  "version": "1.0.0",
  "description": "",
  "main": "dist",
  "scripts": {
    "build": "babel src -s -D -d dist --ignore **/*.test.js --presets env",
    "dev": "babel src -s -D -d dist --ignore **/*.test.js --presets env --watch",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "dependencies": {
    "debug": "^3.1.0",
    "graphql": "^0.13.2",
    "graphql-tag": "^2.9.2",
    "lodash": "^4.17.10",
    "moment-timezone": "^0.5.21",
    "request": "^2.87.0",
    "request-promise-native": "^1.0.5"
  }
}
