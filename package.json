{
  "name": "tidy.surf",
  "private": true,
  "devDependencies": {
    "lerna": "^3.0.0-rc.0"
  },
  "scripts": {
    "dev": "lerna bootstrap --scope app --scope tide && lerna run --parallel dev",
    "heroku-postbuild": "lerna bootstrap && lerna exec --scope app -- npm run build"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "regenerator-runtime": "^0.12.1"
  }
}
